language: node_js
node_js:
  - "lts/*"
# Using a specific yarn version #
# before_install:
  # - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  # - export PATH="$HOME/.yarn/bin:$PATH"
# Caching with other directives
cache:
  yarn: true
